class AppConstants {
  static const String appName = 'SwiftMart';
  static const String baseUrl =
      'https://api.swiftmart.mock'; // Mock URL for now

  // Storage keys, configuration keys
  static const String tokenKey = 'auth_token';
  static const String cachedLocationKey = 'cached_location';

  // Timing
  static const int mockNetworkDelayMs = 800;
}
